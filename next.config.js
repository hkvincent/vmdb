/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["image.tmdb.org"],
  },
  // experimental: {
  //   scrollRestoration: true,
  // },
};

module.exports = nextConfig;
